import { ToolMeta } from '../types'
import { Table } from 'lucide-react'

export const meta: ToolMeta = {
  id: 'markdown-table',
  name: 'Markdown Table',
  description: 'Build Markdown tables visually — add rows and columns, set alignment, and copy the formatted output',
  category: 'Text',
  keywords: ['markdown', 'table', 'generator', 'gfm', 'column', 'row', 'align', 'pipe'],
  path: '/markdown-table',
  icon: Table,
}
