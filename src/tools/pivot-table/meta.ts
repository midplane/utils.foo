import { ToolMeta } from '../types'
import { Table2 } from 'lucide-react'

export const meta: ToolMeta = {
  id: 'pivot-table',
  name: 'Pivot Table',
  description: 'Interactive pivot table for CSV data — aggregate, filter, sort, and visualize with heatmaps',
  category: 'Data',
  keywords: ['pivot', 'table', 'csv', 'aggregate', 'group', 'summarize', 'data', 'analysis', 'heatmap', 'filter', 'sort'],
  path: '/pivot-table',
  icon: Table2,
}
